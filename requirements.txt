# Parallel Risk - Core Dependencies
# Install base environment: pip install -r requirements.txt
# For Phase 1 (RLlib): pip install -r requirements/rllib.txt
# For Phase 2 (TorchRL+GNN): pip install -r requirements/torchrl.txt

-r requirements/base.txt
